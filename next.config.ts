import type { NextConfig } from "next";
import createNextIntlPlugin from 'next-intl/plugin';

// ایجاد پلاگین i18n
const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**.supabase.co',
      },
      {
        protocol: 'https',
        hostname: 'ui-avatars.com',
      },
      // 🔴 حذف شد: الگوی قبلی { protocol: 'https', hostname: '**' }.
      //
      // این خط، همان چیزی است که تقریبا با اطمینان بالا سهمیه‌ی ماهانه‌ی
      // Image Transformation ورسل را خالی می‌کند. آدرس بهینه‌سازی عکس در
      // Next.js (/_next/image?url=...) یک مسیر عمومی روی دامنه‌ی خودِ
      // سایت شماست. وقتی هاست‌نیمِ مجاز "**" (یعنی هر دامنه‌ی https ای در
      // کل اینترنت) باشد، هر بازدیدکننده یا رباتی — حتی بدون این‌که واقعا
      // وارد فروشگاه شده باشد — می‌تواند لینک یک عکسِ حجیم از هر سروری در
      // دنیا را به همین آدرس پاس بدهد و ورسل موظف است آن را دانلود و
      // "بهینه" کند؛ یعنی هر تلاش، یک Transformation از سهمیه‌ی ۵۰۰۰تایی
      // شما را می‌سوزاند. این یک حفره‌ی شناخته‌شده و رایج در پروژه‌های
      // Next.js است.
      //
      // چرا حذفش امن است: طبق کدِ پنل ادمین (app/admin/settings/page.tsx و
      // app/admin/media/page.tsx)، تنها راهِ رسمیِ گذاشتن بنر یا عکس محصول
      // این است که اول از بخش «رسانه» آپلود شود — و آن بخش همیشه روی همان
      // ساب‌دامین Supabase شما ذخیره می‌کند. پس با نگه‌داشتن الگوی
      // '**.supabase.co'، هیچ عکسِ واقعی‌ای در سایت از کار نمی‌افتد.
    ],

    // 🆕 فقط فرمت WebP تولید شود (نه هم AVIF و هم WebP):
    // پیش‌فرض Next.js برای هر عکس هم نسخه‌ی AVIF و هم نسخه‌ی WebP می‌سازد؛
    // یعنی هر عکس عملا ۲ برابر از سهمیه مصرف می‌کند. حجم WebP تقریبا نزدیک
    // به AVIF است و از عکس اصلی (JPEG/PNG آپلودی) به‌مراتب سبک‌تر است، پس
    // این تغییر مصرف را تقریبا نصف می‌کند بدون هیچ افت محسوسی در کیفیت یا
    // سرعت لود.
    formats: ['image/webp'],

    // 🆕 کاهش تعداد سایزهای واسط تولیدی:
    // پیش‌فرض Next.js تا ۸ سایز مختلف عرض (deviceSizes) به‌علاوه ۵ سایز
    // کوچک (imageSizes) برای هر عکس می‌سازد — خیلی از این‌ها (مثلا ۳۸۴۰px
    // برای مانیتور 4K) هیچ‌وقت در این پروژه واقعا لازم نمی‌شوند. لیست زیر
    // دقیقا بر اساس مقادیر واقعیِ sizes که در Hero.tsx، ProductCard.tsx و
    // ProductsSEOContent.tsx استفاده شده انتخاب شده؛ یعنی کیفیت و وضوح عکس
    // روی موبایل/تبلت/دسکتاپ دقیقا مثل قبل می‌ماند، فقط تعداد ترکیب‌های
    // ممکن (و در نتیجه تعداد Transformation های لازم برای هر عکس) کمتر
    // می‌شود.
    deviceSizes: [384, 640, 828, 1080, 1920],
    imageSizes: [256, 384],

    // 🆕 افزایش زمان کش از ۳۰ روز به ۳۶۵ روز:
    // طبق app/admin/media/page.tsx، اسم هر فایلِ آپلودی خودکار و یکتاست
    // (Date.now() + یک عدد رندوم)، پس وقتی ادمین عکسی را عوض می‌کند، آدرس
    // هم عوض می‌شود و هیچ‌وقت عکس قدیمی و جدید با هم قاطی نمی‌شوند. یعنی
    // افزایش این عدد کاملا امن است. نتیجه: هر عکس، به‌جای هر ۳۰ روز یک‌بار،
    // فقط یک‌بار در سال دوباره Transform می‌شود — این به‌تنهایی بخش بزرگی
    // از فشار روی سهمیه‌ی ماهانه را برمی‌دارد، چون بازدیدهای تکراری از
    // همان کش استفاده می‌کنند و چیزی از سهمیه کم نمی‌کنند.
    minimumCacheTTL: 31536000, // 365 روز
  },

  // 🆕 رفع مشکل کش نشدن لوگو / آیکون‌ها / مانیفست در پوشه‌ی public:
  // به‌صورت پیش‌فرض Next.js/Vercel برای فایل‌های داخل پوشه‌ی public
  // هدر Cache-Control: public, max-age=0, must-revalidate می‌فرستد؛
  // یعنی مرورگر کاربر در هر بازدید مجدداً از سرور می‌پرسد «تغییر کرده
  // یا نه» و این باعث کند شدن هدر (که شامل لوگو می‌شود) در هر بار
  // ورود می‌شود، نه فقط بار اول. قانون زیر به این فایل‌ها اجازه می‌دهد
  // در خود مرورگر کاربر تا ۱ سال کش شوند.
  async headers() {
    return [
      {
        source: '/:path(.+\\.(?:ico|png|jpg|jpeg|gif|svg|webp|woff|woff2|ttf|otf))',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=31536000, immutable',
          },
        ],
      },
      {
        source: '/site.webmanifest',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=86400', // ۱ روز؛ چون ممکنه محتواش تغییر کنه
          },
        ],
      },
      {
        // 🆕 سرویس‌ورکر (public/sw.js) هرگز نباید کش شود؛ وگرنه مرورگرِ
        // کاربر برای مدت طولانی (حتی بعد از آپدیت این فایل روی سرور) همان
        // نسخه‌ی قدیمی را اجرا می‌کند و آپدیت‌ها اصلاً به کاربر نمی‌رسند.
        source: '/sw.js',
        headers: [
          {
            key: 'Cache-Control',
            value: 'public, max-age=0, must-revalidate',
          },
        ],
      },
    ];
  },

  // این بخش برای جلوگیری از خطاهای بیلد ESLint
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true, 
  }
};

// کانفیگ اصلی را داخل پلاگین رپ (Wrap) می‌کنیم
export default withNextIntl(nextConfig);